The code actual application code is the same as in the [manual-mock](../manual-mock) example.

The only difference is that we use [testify/mock](https://github.com/stretchr/testify/tree/master/mock) package to mock the API client.
